class Archive < Backup
	# An Archive object represents the most recent snapshot inside the destination (snapshot storage) folder.
	# It gets compressed and copied into the deep-freeze folder for long term storage
end

